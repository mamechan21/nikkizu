<%= form_with(model: nikki, local: true) do |form| %>
  <% if nikki.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nikki.errors.count, "error") %> prohibited this nikki from being saved:</h2>

      <ul>
      <% nikki.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>

  <div class="field">
    <%= form.label :article %>
    <%= form.text_area :article %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
